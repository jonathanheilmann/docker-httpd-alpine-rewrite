# docker-httpd-alpine-rewrite
httpd:alpine Docker image enabled mod_rewrite and `AllowOverride All`.

Thus you can use mod_rewrite and your own `.htaccess`.


## From image
[httpd:alpine](https://hub.docker.com/_/httpd/)
